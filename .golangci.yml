run:
  timeout: 5m
linters:
  enable:
    - bodyclose
    - depguard
    - dogsled
    - errcheck
    - exportloopref
    - gofmt
    - goimports
    - misspell
output:
  format: tab
  print-issued-lines: true
  print-linter-name: true
